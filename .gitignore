# SwiftLint Remote Config Cache
.swiftlint/RemoteConfigCache
.sentryclirc

# swift-format config (downloaded in CI)
.swift-format

# NOTAM API Configuration (contains API token)
SF50 TOLD/NOTAM/NOTAMAPIConfig.xcconfig

# Foundation Models adapter (large ~130MB file, downloaded separately)
SF50 TOLD/Resources/Debug/*.fmadapter

# Training data (large generated files)
Training/.env
Training/data/
